#include <stdio.h>

int main() {
    int base = 3;       
    int exponent = 4;   
    long long result = 1;

    for (int i = 1; i <= exponent; i++) {
        result = result * base;
    }

    printf("%d raised to the power %d is: %lld\n", base, exponent, result);

    return 0;
}
